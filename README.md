# eal-mash-web

Maud, Axum, SeaORM, Supabase, HTMX, and WebSocket Embedded Alerts web server.

Initialized through `DEN-1949` as a testable `mash` foundation. Product behavior continues through focused pull requests.

```bash
python3 scripts/verify_repo.py
```
